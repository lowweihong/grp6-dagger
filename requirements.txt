# Core dependencies for DAgger, BC, SMILe, and Modified DAgger implementations
gymnasium>=0.28.0
numpy>=1.24.0
tensorflow>=2.13.0
stable-baselines3>=2.0.0
tqdm>=4.65.0
opencv-python>=4.7.0

# For Modified DAgger (KDE-based filtering)
scikit-learn>=1.3.0

# For Jupyter notebook evaluation
jupyter>=1.0.0
notebook>=6.5.0

# Additional dependencies (usually installed with above, but listed for completeness)
matplotlib>=3.7.0  # Often used for visualization
pillow>=9.5.0      # Image processing support
gymnasium[box2d]>=0.28.0
